# classifier

The LLM eligibility check. One call per new posting with a description, via OpenRouter, returning a degree-level verdict and a work-authorisation flag that the core loop uses as a soft filter.

## Docs

No docs yet. As they are added, list each one here with a one-line purpose.
